# -*- mode: python ; coding: utf-8 -*-
# Example spec file for PMT with explicit DLL inclusion
# Copy this to PMT.spec and modify as needed

block_cipher = None

a = Analysis(
    ['run_app.py'],
    pathex=[],
    binaries=[
        # If needed, manually add DLL paths here
        # Example: ('C:\\path\\to\\libcrypto-3-x64.dll', '.'),
        # PyInstaller should auto-detect these via hooks, but you can add manually
    ],
    datas=[
        ('app/templates', 'app/templates'),
        ('app/static', 'app/static'),
        ('docs', 'docs'),
    ],
    hiddenimports=[
        'dotenv',
        'Jinja2',
        'cryptography.hazmat.bindings._rust',
    ],
    hookspath=['hooks'],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    win_no_prefer_redirects=False,
    win_private_assemblies=False,
    cipher=block_cipher,
    noarchive=False,
)

pyz = PYZ(a.pure, a.zipped_data, cipher=block_cipher)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.zipfiles,
    a.datas,
    [],
    name='PMT',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=False,  # Set to True if you want console window
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
    icon='favicon.ico',  # 아이콘 파일 경로
    version='version.txt',  # 버전 정보 파일 경로
)

